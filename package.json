{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.6.2"
  },
  "workspaces": [
    "website/**"
  ],
  "scripts": {
    "update:deps": "npx -y taze -r -w && lerna bootstrap && npx yarn-deduplicate yarn.lock && yarn"
  }
}
